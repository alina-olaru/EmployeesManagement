import { FilterEmployeePipe } from './filter-employee.pipe';

describe('FilterEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});
